export const metadata = {
  title: "ELYXIER Studio",
  description: "Content studio for elyxier.com",
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
